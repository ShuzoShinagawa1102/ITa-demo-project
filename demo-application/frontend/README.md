# Demo Shopping Frontend

## Prerequisites

- Node.js 18+
- Backend running on `http://localhost:8080`

## Run

```sh
cd frontend
npm install
npm run dev
```

If backend is not `http://localhost:8080`, set:

```sh
set VITE_API_BASE_URL=http://localhost:8080
```

